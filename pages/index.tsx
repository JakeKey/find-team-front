import Head from 'next/head';

const Home: React.FC = () => {
  const test = 'asd';

  return (
    <div>
      <Head>
        <title>Create Next App</title>
        <link rel="icon" href="/favicon.ico" />
      </Head>
      index
    </div>
  );
};

export default Home;
